class edge:
    def __init__(self):
        self.windows = {}

    def processTweet(tweet):
        ## extract individual components of the tweet
        date = tweet.date

        text = tweet.text

        ## get window period (rn default to month)
        